# Vercel deployment config (local only)
.vercel

# Environment variables (NEVER commit)
.env
.env.local
.env.*.local

# OS files
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
*.swp
*.swo

# Node (if added later)
node_modules/
npm-debug.log*

# Build output (if added later)
dist/
build/
